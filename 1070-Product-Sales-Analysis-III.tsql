1/* Write your T-SQL query statement below */
2select product_id, year as first_year, quantity, price from
3	(select *,
4			DENSE_RANK() over (partition by product_id order by year asc) as DR
5	from Sales) as temp_1
6where DR = 1
